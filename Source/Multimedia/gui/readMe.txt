Button.cpp

    	*As in BGO, the update body remains empty
	* Added boolean for ToggleEnabled


Label.cpp

	*Added boolean for ToggleBackground


ValueButton.cpp
	
	* Destructor, not sure what to "destroy"
	* SetFormat, not sure what are the parameters given 
	what to set.

Container.cpp

	* Expecting the first GO on the constructor to be the location of where to put the container.
	* Not sure what pack is suppose to do

BoolButton.cpp
	
	*As in BGO, the update body remains empty

TextButton.cpp

	* setFormat is not clear as to what are we formatting and
	what that parameters are used for aside from color
	*Destructor not clear as to what to 'destroy'