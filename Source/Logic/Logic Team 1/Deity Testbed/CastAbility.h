#ifndef CAST_ABILITY
#define CAST_ABILITY

#include <string>
#include <map>

typedef std::string Aspect;
typedef std::string Ability;

void testBed(Aspect first, Aspect second);
void initAspects();
void displayAspects(Aspect& first, Aspect& second);

#endif
